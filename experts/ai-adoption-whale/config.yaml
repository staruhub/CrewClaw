temperature: 0.4

toolsets:
  - web

platform_toolsets:
  cli:
    - web
    - no_mcp

coding_context: off

agent:
  disabled_toolsets: [browser, code_execution, file, skills, terminal]

plugins:
  enabled: []

approvals:
  mode: manual
