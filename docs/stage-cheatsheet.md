# CrewClaw · ClawCon Stage Cheat Sheet

> Terminal must be in `C:\Users\12117\Playground\crewclaw\crewhire`. `crew` is on PATH.

## Open (no self-intro — fire first)
> "I don't demo a tool. I hire an AI employee — live. And if it underperforms, I fire it."

## The four commands (in order)

```
crew hire macao-networking-agent
```
↳ onboarding ceremony · 4 ✓ · scripted, can't fail

```
crew badge macao-networking-agent
```
↳ the manifest as an ID card · "No manifest = a tool. With a manifest = an employee."

```
crew standup "It's launch morning at ClawCon — I pitch the judges in 1 hour. What should each of you do right now?"
```
↳ ★ THE MOMENT · 3 employees, one brief, in parallel · real ~2.7x faster · real ~$0.04
↳ "Not an AI writing text — a parallel AI workforce. 3 people's work in 8 seconds for 4 cents."

```
crew verify
```
↳ safety net · scripted · no network · use this first if venue wifi is dead

## Close (call to action, not "thank you")
> "Don't install another tool. **Hire your first AI employee.**"

## The one line judges must remember
> Others build agent tools. **You built the agent labor market.**

---

## If something breaks
- emoji boxes → add `--ascii` to any command
- standup hangs/slow → 45s timeout fails it clean; show `docs/stage-fallback-standup.md` or run `crew verify`
- speedup number changes (2.7x–2.9x) → say "it changes every run — because it's real"

## Optional flair
- `crew run code-review-shrimp "review this diff: <paste>"` → catches a real SQL injection
- `crew fire macao-networking-agent` → the payoff for the opening joke
